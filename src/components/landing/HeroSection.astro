---
import AnimatedText from "../AnimatedText.astro";
import { ArrowDown } from "@lucide/astro";
---

<header
  class="relative w-full h-screen min-h-150 flex items-center justify-start overflow-hidden"
>
  <div class="absolute inset-0 z-0">
    <img
      src="https://images.unsplash.com/photo-1486406146926-c627a92ad1ab?q=80&w=2070&auto=format&fit=crop"
      alt="Reunión Corporativa Estratégica"
      class="w-full h-full object-cover"
    />
    <div class="absolute inset-0 hero-overlay"></div>
  </div>

  <div class="relative z-10 max-w-7xl mx-auto px-6 lg:px-12 w-full pt-20">
    <div class="max-w-3xl">
      <AnimatedText
        as="h1"
        class="text-4xl md:text-6xl lg:text-7xl text-white font-semibold leading-[1.1] tracking-tight mb-8"
      >
        Estructuración jurídica para <span
          class="text-transparent bg-clip-text bg-linear-to-r from-red-400 to-red-600 italic font-serif pr-2"
          >inversiones</span
        > de alto impacto.
      </AnimatedText>
      <AnimatedText
        as="p"
        class="text-lg md:text-xl text-slate-300 font-normal max-w-2xl leading-relaxed mb-10"
      >
        Asesoría legal especializada para empresas. Dotamos a cada
        proyecto de una estructura clara, previsible y ejecutable.
      </AnimatedText>
      <div class="flex flex-col sm:flex-row gap-4">
        <a
          href="#contacto"
          class="inline-flex justify-center items-center px-8 py-4 bg-white text-slate-900 text-sm font-semibold hover:bg-slate-100 transition-colors duration-300 min-w-40"
        >
          Agendar Consulta
        </a>
        <a
          href="#services"
          class="inline-flex justify-center items-center px-8 py-4 border border-white/30 text-white text-sm font-semibold hover:bg-white/10 transition-colors duration-300 min-w-40"
        >
          Nuestros Servicios
        </a>
      </div>
    </div>
  </div>

  <div
    class="absolute bottom-10 left-1/2 transform -translate-x-1/2 animate-bounce"
  >
    <ArrowDown width="24" stroke-width="1.5" class="text-white/50" />
  </div>
</header>

<style>
  .hero-overlay {
    background: linear-gradient(to right, #0f1729f2, rgba(15, 23, 42, 0.7));
  }
</style>
