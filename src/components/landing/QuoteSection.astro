---
import { Scale, Quote } from "@lucide/astro";
import AnimatedText from "../AnimatedText.astro";
---

<section class="py-20 bg-slate-900 text-white relative overflow-hidden">
  <div class="absolute top-0 right-0 p-20 opacity-5">
    <Scale width="400" stroke-width="1" />
  </div>
  <div class="max-w-4xl mx-auto px-6 lg:px-12 text-center relative z-10">
    <Quote
      width="32"
      stroke-width="1.5"
      class="text-red-500 mb-6 opacity-80"
    />
    <AnimatedText
      as="h2"
      class="text-2xl md:text-4xl font-serif font-normal leading-relaxed tracking-tight italic"
    >
      "Integramos derecho, estrategia y gestión de riesgos para que
      empresas e inversionistas tomen decisiones sólidas, ejecuten
      proyectos con previsibilidad y protejan su valor en el tiempo."
    </AnimatedText>
    <div class="w-full flex justify-end mt-6">
      <Quote
        width="32"
        stroke-width="1.5"
        class="text-red-500 opacity-80"
      />
    </div>
    <div class="mt-10 flex justify-center">
      <div class="w-20 h-px bg-slate-700"></div>
    </div>
  </div>
</section>
